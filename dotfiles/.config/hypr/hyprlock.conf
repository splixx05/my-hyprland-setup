#  _   _                       _            _    
# | | | |_   _ _ __   ___ _ __| | ___   ___| | __
# | |_| | | | | '_ \ / _ \ '__| |/ _ \ / __| |/ /
# |  _  | |_| | |_) |  __/ |  | | (_) | (__|   < 
# |_| |_|\__, | .__/ \___|_|  |_|\___/ \___|_|\_\
#        |___/|_|                                
#
#--------------------------------------------------
#---------  Screenlock configuration  ------------- 
#--------------------------------------------------

general {
    ignore_empty_input = true
    hide_cursor = false
}

background {
    monitor =
    path = $HOME/.config/hypr/src/lock2.jpg    # If you like a pic as bg, uncomment this section
    # path = screenshot  # If you like a simply blur effect on the current window, uncomment this section 
    color = rgba(25, 20, 20, 0.7)
    blur_passes = 0                             # former val = 3 
    contrast = 0.7                                # former val = 1
    brightness = 1                              # former val = 0.7
    vibrancy = 1.3                              # former val = 0.9
    vibrancy_darkness = 1                       # former val = 0.7
}

animations {
  bezier = linear, 0.99, 0.4, 0, 0.49
  animation = fade, 1
  animation = fadeIn, 1, 3, linear
  animation = fadeOut, 1, 3, linear
  animation = inputFieldFade, 1, 1, linear
  animation = inputFieldDots, 1, 1, linear
}

# Avatar frame
image {
    monitor =
    path = $HOME/.config/hypr/src/usr.jpg 
    size = 150 # lesser side if not 1:1 ratio
    rounding = -1 # negative values mean circle
    border_size = 2
    border_color = rgba(7898c766)
    rotate = 0 # degrees, counter-clockwise
    reload_time = -1 # seconds between reloading, 0 to reload with SIGUSR2
#    reload_cmd =  # command to get new path. if empty, old path will be used. don't run "follow" commands like tail -F
    position = -170, 70
    halign = center
    valign = center
    shadow_passes = 10
    shadow_size = 20
    shadow_color = rgb(0,0,0)
    shadow_boost = 1.6
}

# Login box (bg)
shape {
    monitor =
    size = 550, 350
    color = rgba(25, 26, 52, 0.97)
    rounding = 0
    border_size = 3
    border_color = rgba(7898c733)
    rotate = 0
    xray = false # if true, make a "hole" in the background (rectangle of specified size, no rotation)
    position = -20, 0
    halign = center
    valign = center
}

# Username
# label {
#     monitor =
#     text = cmd[update:1000] echo "$(~/.config/hypr/conf.d/scripts/whoami.sh)"
#     color = rgb(3ea5d9)
#     font_size = 15
#     font_family = Pirulen
#     position = 75, 35
#     halign = center
#     valign = center
# }

# Input PW
input-field {
    monitor =
    size = 430, 50
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
    outer_color = rgba(7898c766)
    inner_color = rgba(0d2a5699)
    font_color = rgb(3ea5d9)
    fade_on_empty = false
    fade_timeout = 5000 # Milliseconds before fade_on_empty is triggered.
    placeholder_text = <i><b> </b></i> # Text rendered in the input box when it's empty.
    hide_input = false
    rounding = 50 # -1 means complete rounding (circle/oval)
    check_color = rgb(605DB0)
    fail_color = rgb(f7768e) # if authentication failed, changes outer_color and fail message color
    # fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
    fail_text = <b>Nice try 🖕</b> # can be set to empty
    fail_transition = 500 # transition time in ms between normal outer_color and fail_color
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below
    position = -20, -100
    halign = center
    valign = center
    shadow_passes = 10
    shadow_size = 20
    shadow_color = rgb(0,0,0)
    shadow_boost = 1.6
}

# Date - Day
label {
    monitor =
    text = cmd[1000] echo "$(date +"%a")  -"
    color = rgba(7898c788)
    font_size = 18
    font_family = Pirulen
    position = -20, 25
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

# Date - Year
label {
    monitor =
    text = cmd[1000] echo "$(date +"%d.%m.%y")"
    color = rgba(7898c788)
    font_size = 18
    font_family = Pirulen
    position = 117, 25
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

# Clock
label {
    monitor =
    text = cmd[4:1000] echo "$TIME"
    color = rgba(7898c788)
    font_size = 52
    font_family = Pirulen
    position = 60, 90
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}


#---------------------------------------------------------
#------------------  Reserve  ----------------------------
#---------------------------------------------------------

# # Login frame
# shape {
#     monitor =
#     size = 400, 700
#     color = rgba(255, 255, 255, .0)
#     rounding = -1
#     rotate = 90
#     border_size = 0
#     blur_passes = 5
#     contrast = 1
#     brightness = 0.5
#     vibrancy = 0.3
#     vibrancy_darkness = 0.7
#     # border_color = rgba(255, 255, 255, 0)
#     xray = false # if true, make a "hole" in the background (rectangle of specified size, no rotation)
#     position = 0, -10
#     halign = center
#     valign = center
# }

# # Host
# label {
#     monitor =
#     text = cmd[update:1000] echo
#     color = rgb(7898c7)
#     font_size = 15
#     font_family = Pirulen
#     position = -45, 120
#     halign = center
#     valign = center
# }

# # Profile Picture
# image {
#     monitor =
#     path = 
#     size = 100
#     border_size = 2
#     border_color = $foreground
#     position = 0, -100
#     halign = center
#     valign = center
# }
#
# # Desktop Environment
# image {
#     monitor =
#     path = png
#     size = 75
#     border_size = 2
#     border_color = $foreground
#     position = -50, 50
#     halign = right
#     valign = bottom
# }
#
# 
# Battery status
# label {
#     monitor =
#     text = cmd[update:1000] echo 
#     color = $foreground
#     font_size = 24
#     font_family = JetBrains Mono
#     position = -90, -10
#     halign = right
#     valign = top
# }
#
#
