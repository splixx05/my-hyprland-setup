
# === nnn CONFIG ===
# Tokyo Night-inspired colors
export NNN_COLORS='4466'          # play around later
export NNN_CONTEXT_COLORS='1234'  # colors for multi-context (tabs)

# File management
export NNN_OPENER=xdg-open        # use linux "open" command
export NNN_TRASH=1                # safe delete (moves to trash)
export NNN_OPTS='deH'             # details (-d), auto-enter dirs (-e), show hidden (-H)

# Plugins
export NNN_PREVIEW=kitty
export NNN_PLUG='p:preview-tui;'
export NNN_FIFO=/tmp/nnn.fifo

# Alias
# alias n='nnn'
# alias ls='nnn -de'

